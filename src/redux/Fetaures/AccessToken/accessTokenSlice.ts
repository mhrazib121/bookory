import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  accessToken: "",
};

const accessTokenSlice = createSlice({
  name: "accessToken",
  initialState,
  reducers: {
    // setAcces
  },
});
